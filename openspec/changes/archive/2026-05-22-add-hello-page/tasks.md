## 1. Tab structure

- [x] 1.1 Add「首页」as the first `st.tabs` label in `app.py`
- [x] 1.2 Implement `_render_hello_tab()` and wire `with tab_home` block

## 2. Home content

- [x] 2.1 Show product intro and numbered workflow steps on the home tab
- [x] 2.2 Show read-only API/KB/mode status using `config` and session state

## 3. Cleanup

- [x] 3.1 Remove duplicate main-area `st.info` recommendation (content lives on home tab)

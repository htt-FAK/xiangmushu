## 1. 创建 openspec-self 规范

- [x] 1.1 创建 `openspec/specs/openspec-self/spec.md`，包含框架定位、目录结构、config.yaml 格式、spec 格式、变更提案格式、语言约定
- [x] 1.2 验证文件结构（目录存在、文件不空、YAML 元数据正确）

## 2. 创建变更归档

- [x] 2.1 创建 `openspec/changes/archive/2026-05-23-add-openspec-self/` 目录
- [x] 2.2 写入 `.openspec.yaml`（schema + 创建日期）
- [x] 2.3 写入 `proposal.md`（变更动机、内容、影响范围）
- [x] 2.4 写入 `design.md`（上下文、目标/非目标、决策、风险）
- [x] 2.5 写入 `tasks.md`（本任务清单）
